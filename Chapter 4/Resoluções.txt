Chapter 4 Self Test

1. What is the difference between a class and an object?
R: A class is a template for you to create an object

2. How is a class defined?
R: Public class [name]

3. What does each object have its own copy of?
R: When you create multiple objects from a class, each object gets its own unique copy of the instance variables defined in the class.

4. Using two separate statements, show how to declare an object called counter of a class
called MyCounter.
R: 
MyCounter counter = new MyCounter();
MyCounter counter;

5. Show how a method called myMeth( ) is declared if it has a return type of double and has
two int parameters called a and b.
R: public static boolean myMeth(int a, int b){}

6. How must a method return if it returns a value?
R: it must have the "return value" expression

7. What name does a constructor have?
R: The name of the classe

8. What does new do?
R: It makes a pointer to a part of the memory, indicating that it shall receive a object, so it returns an reference to that object

9. What is garbage collection, and how does it work? What is finalize( )?
R: Garbage collection is a name given to a job, whom aim is free memory, preveting the overflow in the heap

10. What is this?
R: "this" is a keyword used to refence to that object being used

11. Can a constructor have one or more parameters?
R: It can have as much as you decided

12. If a method returns no value, what must its return type be?
R: void

